import { VisHostDirective } from './vis-host.directive';

describe('VisHostDirective', () => {
  it('should create an instance', () => {
    const directive = new VisHostDirective();
    expect(directive).toBeTruthy();
  });
});
