export interface StoreData {
  // data organized in maps.
  // participants: {[key:number]: Participant};
  // threads: {[key:number]: Thread};
  // messages: {[key:number]:Message};
}



export const InitialStoreData: StoreData = {
  // threads: {},
  // messages: {},
  // participants: {}
};
